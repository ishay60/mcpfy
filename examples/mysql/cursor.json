{
  "mcpServers": {
    "mcpolyglot": {
      "command": "npx",
      "args": [
        "-y",
        "@mcpolyglot/cli",
        "serve",
        "--config",
        "/abs/path/to/examples/mysql/mcpolyglot.config.ts"
      ],
      "env": {
        "DATABASE_URL": "mysql://user:pass@localhost:3306/app"
      }
    }
  }
}
