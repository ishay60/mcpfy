# Changesets

Run `pnpm changeset` to record a version bump for any package you change. CI will refuse to merge a behavior-change PR without a changeset.

See <https://github.com/changesets/changesets> for documentation.
